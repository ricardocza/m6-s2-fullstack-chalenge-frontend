import styled from 'styled-components'

export const StyledRegister = styled.div`
   
        & main > form {
            width: 90%;
            margin: 1rem auto;
            display: flex;
            flex-direction: column;
            gap: 16px;
        }
    
`

